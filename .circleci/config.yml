version: 2.1
jobs:
  build-and-push:
    executor: docker/docker
    steps:
      - setup_remote_docker
      - checkout
      - docker/check
      - docker/build:
          image: hengel2810/se09-device-service
      - docker/push:
          image: hengel2810/se09-device-service
orbs:
  docker: circleci/docker@0.5.20
workflows:
  commit:
    jobs:
      - build-and-push:
          context: SE09
